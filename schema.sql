DROP TABLE IF EXISTS Movies;


CREATE TABLE IF NOT EXISTS Movies (
    movieID varchar(255),
    movieName varchar(255),
    movieLength varchar(255),
    type varchar(255),
    realsedate varchar(255)
);